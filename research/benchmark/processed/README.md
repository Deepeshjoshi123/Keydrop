# Legacy Processed Data

These summaries predate the manifest-backed workflow and may aggregate multiple
unrelated runs. New summaries are written inside the relevant study directory
by `research/benchmark/scripts/process_results.py --study <path>`.
